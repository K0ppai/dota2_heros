const Form = () => (
  <>
    <form role="search" className="m-2">
      <input type="search" placeholder="type hero name.." className="bg-black" />
      <select name="" id="">
        <option value="">nuker</option>
      </select>
    </form>
  </>
);

export default Form;
